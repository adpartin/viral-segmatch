# STATUS: active -- H3N2 + concat; known to fail (AUC≈0.50)
# Flu schema-ordered (HA -> NA), raw mode with slot-specific LayerNorm, concat, filtered to H3N2

defaults:
  - flu_schema_raw_slot_norm_concat
  - _self_

dataset:
  hn_subtype: "H3N2"

training:
  patience: 50 # Default is 15
