# STATUS: active -- year=2024 filter, concat
# Flu schema-ordered (HA -> NA), raw mode with slot-specific LayerNorm, concat, filtered to year 2024

defaults:
  - flu_schema_raw_slot_norm_concat
  - _self_

dataset:
  year: 2024
