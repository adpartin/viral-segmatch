# STATUS: experimental -- k-mer + concat on H3N2-only; does concat collapse with k-mers too?
# Flu schema-ordered (HA -> NA), raw k-mer features (4096-dim), concat, H3N2 filter
# ESM-2 concat fails on H3N2 (AUC≈0.50); k-mers may behave differently.

defaults:
  - flu_schema_raw_kmer_k6_slot_norm_concat
  - _self_

dataset:
  hn_subtype: "H3N2"

training:
  patience: 50
