# STATUS: infeasible -- bipartite collapse at id090 on the full-corpus HA/NA pool.
# Per-function counts (HA=110, NA=108 clusters) suggest workable structure but
# the bipartite graph has only 8 components with the largest covering 99.3%
# of deduped pairs. Stage 3 will produce a ~99 / 0.5 / 0.5 split.
# Kept for reference; not run. See
# results/flu/July_2025/runs/cluster_disjoint_feasibility/feasibility_ha_na_aa.csv.

defaults:
  - flu_ha_na
  - _self_

dataset:
  split_strategy:
    mode: cluster_disjoint
    cluster_id_path: data/processed/flu/July_2025/clusters_aa/t090/combined_cluster.parquet
    cluster_id_threshold: 0.90
