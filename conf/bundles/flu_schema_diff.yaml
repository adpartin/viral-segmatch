# Flu schema-ordered dataset (HA -> NA) with pair features = diff-only
#
# Inherits schema ordering + function selection from `flu_schema.yaml`.

defaults:
  - flu_schema
  - _self_

training:
  use_concat: false
  use_diff: true
  use_prod: false
  eval_swapped_test: true
