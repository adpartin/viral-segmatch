# STATUS: experimental -- HA/NA cluster_disjoint id099 at neg_to_pos_ratio=3.0.
# Inherits flu_ha_na_cluster_id99 (cluster_disjoint at aa identity >= 0.99).
# Tripled negative count vs the r=1.5 parent to harden the decision boundary
# against the "feature-rhymes-with-positive" FP failure mode observed at r=1.5
# (HA/NA cluster_id99 MLP precision = 0.69 with 2,357 FPs on test).
#
# See:
#   docs/results/2026-05-14_cluster_disjoint_id99_results.md
#   docs/plans/2026-05-08_cosine_and_cluster_splits_plan.md (Experiment B)

defaults:
  - flu_ha_na_cluster_id99
  - _self_

dataset:
  neg_to_pos_ratio: 3.0
