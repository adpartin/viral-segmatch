# STATUS: experimental -- HA/NA 2D-CD CV on the nt_cds (CDS DNA) axis.
# Built by src/datasets/dataset_pairs_cc.py.

defaults:
  - flu_ha_na_cc_aa
  - _self_

dataset:
  split_strategy:
    cluster_alphabet: nt_cds
    # threshold ('t099') is derived from the cluster_id_path parent dir name
    cluster_id_path: data/processed/flu/July_2025/clusters_nt_cds/t099/combined_cluster.parquet
    cluster_id_threshold: 0.99
