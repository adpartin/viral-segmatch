# STATUS: experimental -- k-mer + unit_diff on H3N2-only; patience=40 for delayed learning
# Flu schema-ordered (HA -> NA), raw k-mer features (4096-dim), unit_diff, H3N2 filter
# Comparison: does k-mer unit_diff succeed on homogeneous data like ESM-2 does?

defaults:
  - flu_schema_raw_kmer_k6_slot_norm_unit_diff
  - _self_

dataset:
  hn_subtype: "H3N2"

training:
  patience: 50
