# STATUS: legacy (Gen 1) -- H3N2 + diff, no schema ordering
# flu_h3n2 bundle with pair features = diff-only (no concat, no prod)

defaults:
  - flu_h3n2
  - _self_

training:
  interaction: diff
  eval_swapped_test: true