# STATUS: active -- Human host filter, concat
# Flu schema-ordered (HA -> NA), raw mode with slot-specific LayerNorm, concat, filtered to Human host

defaults:
  - flu_schema_raw_slot_norm_concat
  - _self_

dataset:
  host: "Human"
