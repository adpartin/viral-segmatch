# STATUS: experimental -- k-mer baseline (k=6, HA/NA, no filter)
# Flu schema-ordered (HA -> NA), raw k-mer features (4096-dim), unit_diff interaction
# Apple-to-apple comparison: same pairs as ESM-2 bundles, different features.

defaults:
  - flu_schema
  - /kmer: default
  - _self_

training:
  feature_source: kmer
  interaction: unit_diff
  pre_mlp_mode: slot_norm
