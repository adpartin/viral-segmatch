# Overfit bundle (HA/NA, 5k isolates cap)
#
# Inherit the default Flu task (currently HA/NA selection etc.) from `flu.yaml`.

defaults:
  - flu
  - _self_

dataset:
  max_isolates_to_process: 5000
  train_ratio: 0.005
  val_ratio: 0.5

training:
  epochs: 120
  patience: 120
  dropout: 0.0
