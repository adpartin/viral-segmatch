# STATUS: legacy (Gen 1 base) -- H3N2 filter base, no schema ordering
# Flu metadata filtering experiment
# Filter isolates to H3N2 before creating train/val/test splits.
#
# Inherit the default Flu task (currently HA/NA selection etc.) from `flu.yaml`.

defaults:
  - flu
  - _self_

dataset:
  hn_subtype: "H3N2"
