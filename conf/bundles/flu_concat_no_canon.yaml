# flu_concat bundle variant with canonicalize_pair_orientation DISABLED
# Use this for ablation vs flu_concat (default canonicalization enabled).

defaults:
  - flu_concat
  - _self_

dataset:
  canonicalize_pair_orientation: false

training:
  use_concat: true
  use_diff: false
  use_prod: false
  eval_swapped_test: true
