# STATUS: experimental -- slot-specific slot transform; not completed
# Flu schema-ordered (HA -> NA), raw mode with slot-specific slot transforms

defaults:
  - flu_schema
  - _self_

training:
  interaction: diff
  slot_transform: slot_specific
  slot_transform_dims: [1280, 512, 256]
