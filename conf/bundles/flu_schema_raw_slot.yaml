# Flu schema-ordered (HA -> NA), raw mode with slot-specific pre-MLPs

defaults:
  - flu_schema
  - _self_

training:
  interaction: diff
  pre_mlp_mode: slot_specific
  pre_mlp_dims: [1280, 512, 256]
