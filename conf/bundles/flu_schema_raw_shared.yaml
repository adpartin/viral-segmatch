# Flu schema-ordered (HA -> NA), raw mode with shared pre-MLP

defaults:
  - flu_schema
  - _self_

training:
  input_mode: raw # interaction | raw
  interaction: diff # concat | diff | prod | concat+diff+prod
  pre_mlp_mode: shared
  pre_mlp_dims: [1280, 512, 256]
