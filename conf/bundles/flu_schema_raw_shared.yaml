# STATUS: experimental -- shared slot transform; not completed
# Flu schema-ordered (HA -> NA), raw mode with shared slot transform

defaults:
  - flu_schema
  - _self_

training:
  interaction: diff
  slot_transform: shared
  slot_transform_dims: [1280, 512, 256]
