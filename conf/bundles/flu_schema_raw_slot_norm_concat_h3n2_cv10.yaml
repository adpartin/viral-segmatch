# STATUS: active -- paper experiment (10-fold CV, slot_norm + concat, HA/NA, H3N2-only)
# Cross-validation extension of flu_schema_raw_slot_norm_concat_h3n2.
# Inherits all settings; adds n_folds=10 for CV dataset generation.
#
# Note: This bundle must stay flat in conf/bundles/ (not in a subdirectory).

defaults:
  - flu_schema_raw_slot_norm_concat_h3n2
  - _self_

dataset:
  n_folds: 10
