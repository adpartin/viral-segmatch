# STATUS: active -- Illinois filter, concat
# Flu schema-ordered (HA -> NA), raw mode with slot-specific LayerNorm, concat, filtered to Illinois

defaults:
  - flu_schema_raw_slot_norm_concat
  - _self_

dataset:
  geo_location: "Illinois"
