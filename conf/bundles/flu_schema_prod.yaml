# Flu schema-ordered dataset (HA -> NA) with pair features = prod-only
#
# Inherits schema ordering + function selection from `flu_schema.yaml`.

defaults:
  - flu_schema
  - _self_

training:
  use_concat: false
  use_diff: false
  use_prod: true
  eval_swapped_test: true
