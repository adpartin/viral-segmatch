# Flu schema-ordered (HA -> NA), raw mode with slot-specific LayerNorm, unit_diff, filtered to Illinois

defaults:
  - flu_schema_raw_slot_norm_unit_diff
  - _self_

dataset:
  geo_location: "Illinois"
