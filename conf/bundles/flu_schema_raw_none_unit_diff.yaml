# STATUS: ablation -- no LayerNorm; baseline vs slot_norm
# Flu schema-ordered (HA -> NA), raw mode with NO slot transform/LayerNorm, unit_diff interaction
#
# Ablation bundle: tests whether unit_diff works without slot-specific LayerNorm.
# Compare with flu_schema_raw_slot_norm_unit_diff to isolate LayerNorm's contribution.

defaults:
  - flu_schema
  - _self_

training:
  interaction: unit_diff
  slot_transform: none
