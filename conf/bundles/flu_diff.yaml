# STATUS: legacy (Gen 1) -- diff interaction, no schema ordering
# flu bundle with pair features = diff-only (no concat, no prod)

defaults:
  - flu
  - _self_

training:
  interaction: diff
  eval_swapped_test: true
