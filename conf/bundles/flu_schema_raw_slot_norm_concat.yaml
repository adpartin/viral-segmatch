# Flu schema-ordered (HA -> NA), raw mode with slot-specific LayerNorm

defaults:
  - flu_schema
  - _self_

training:
  # input_mode: raw # interaction | raw
  # interaction: concat # concat | diff | prod | concat+diff+prod
  pre_mlp_mode: slot_norm
  # pre_mlp_dims: null
