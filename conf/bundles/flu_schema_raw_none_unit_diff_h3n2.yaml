# Flu schema-ordered (HA -> NA), raw mode with NO pre-MLP/LayerNorm, unit_diff, H3N2 only
#
# Ablation bundle: tests whether unit_diff works on homogeneous data without LayerNorm.
# Compare with flu_schema_raw_slot_norm_unit_diff_h3n2.

defaults:
  - flu_schema_raw_none_unit_diff
  - _self_

dataset:
  hn_subtype: "H3N2"

training:
  patience: 100 # Default is 15
