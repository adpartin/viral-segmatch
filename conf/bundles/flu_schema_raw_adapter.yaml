# STATUS: experimental -- adapter slot transform; not completed
# Flu schema-ordered (HA -> NA), raw mode with shared trunk + slot adapters

defaults:
  - flu_schema
  - _self_

training:
  interaction: diff
  slot_transform: shared_adapter
  slot_transform_dims: [1280, 512, 256]
  adapter_dims: [128]
